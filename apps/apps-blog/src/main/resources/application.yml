spring:
  profiles:
    active: proc
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
  http:
    encoding:
      charset: UTF-8
mybatis:
  mapper-locations: classpath:mapper/**.xml
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 80
  servlet:
    context-path: /blog
logging:
  level:
    root: info